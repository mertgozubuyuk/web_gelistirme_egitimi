const urun = "elma";
const adet = 3;
const fiyat = 4;

// Merhaba, 3 tane elma 12TL'dir.
const urunBilgi = `Merhaba , ${adet} tane ${urun} ${(adet * fiyat)} TL'dir.`;

console.log(urunBilgi);



// Toyota'nın hızı 60km/s'dir. 3 saatte 180km yol alır.

const model = "Mercedes";
const hız = 60;
const saat = 3;
const aldigiYol = (hız*saat);

const bilgi = `${model} hızı ${hız}km/s'dir. ${saat} saatte ${aldigiYol}km yol alır.`;

console.log(bilgi);
